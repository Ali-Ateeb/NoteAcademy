# Node
node_modules/
.next/
.next-verify/
out/
*.tsbuildinfo
next-env.d.ts

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Env
.env
.env.local
.env*.local

# Pipeline working data. Anchored to the repo root (leading slash) — a bare
# `papers/` also matched web/src/app/papers/, a real tracked Next.js route,
# and made `git add` on anything under it warn that it was ignored.
pipeline/work/
pipeline/out/
/papers/
*.pdf

# OS / editor
.DS_Store
.idea/
.vscode/
